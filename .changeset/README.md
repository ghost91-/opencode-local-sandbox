# Changesets

This folder is managed by the changeset tool. When you run `pnpm changeset add`, a new markdown file will be created here describing the change and its semver impact.

These files are consumed by `changeset version` during the release process and then deleted.
